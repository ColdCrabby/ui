// This file is auto-generated by @hey-api/openapi-ts

import {
  type Client,
  type ClientMeta,
  formDataBodySerializer,
  type Options as Options2,
  type RequestResult,
  type TDataShape,
} from './client';
import { client } from './client.gen';
import type {
  ClaimUploadData,
  ClaimUploadErrors,
  ClaimUploadResponses,
  GetHealthData,
  GetHealthErrors,
  GetHealthResponses,
  GetPresetData,
  GetPresetErrors,
  GetPresetResponses,
  GetUploadData,
  GetUploadErrors,
  GetUploadResponses,
  ListVendorsData,
  ListVendorsErrors,
  ListVendorsResponses,
  SearchPresetsData,
  SearchPresetsErrors,
  SearchPresetsResponses,
  UploadPresetsData,
  UploadPresetsErrors,
  UploadPresetsResponses,
} from './types.gen';

export type Options<
  TData extends TDataShape = TDataShape,
  ThrowOnError extends boolean = boolean,
  TResponse = unknown,
> = Options2<TData, ThrowOnError, TResponse> & {
  /**
   * You can provide a client instance returned by `createClient()` instead of
   * individual options. This might be also useful if you want to implement a
   * custom client.
   */
  client?: Client;
  /**
   * You can pass arbitrary values through the `meta` object. This can be
   * used to access values that aren't defined as part of the SDK function.
   */
  meta?: keyof ClientMeta extends never ? Record<string, unknown> : ClientMeta;
};

/**
 * Liveness and readiness
 *
 * Reports process liveness, catalog readiness, the served catalog revision, and the time of the last successful ingest.
 */
export const getHealth = <ThrowOnError extends boolean = false>(
  options?: Options<GetHealthData, ThrowOnError>,
): RequestResult<GetHealthResponses, GetHealthErrors, ThrowOnError> =>
  (options?.client ?? client).get<GetHealthResponses, GetHealthErrors, ThrowOnError>({
    url: '/v1/health',
    ...options,
  });

/**
 * Search or browse the preset catalog
 *
 * Searches the in-memory catalog of presets ingested from ColdCrabby/presets. Returns a page of summaries for rendering result rows.
 */
export const searchPresets = <ThrowOnError extends boolean = false>(
  options?: Options<SearchPresetsData, ThrowOnError>,
): RequestResult<SearchPresetsResponses, SearchPresetsErrors, ThrowOnError> =>
  (options?.client ?? client).get<SearchPresetsResponses, SearchPresetsErrors, ThrowOnError>({
    url: '/v1/presets',
    ...options,
  });

/**
 * Fetch a complete preset in the slicer's profile shape
 *
 * Returns the complete preset identified by id, with source "catalog" and a canonical import_url. This is the exact JSON the slicer can consume directly, so a client imports one preset without downloading the whole catalog.
 */
export const getPreset = <ThrowOnError extends boolean = false>(
  options: Options<GetPresetData, ThrowOnError>,
): RequestResult<GetPresetResponses, GetPresetErrors, ThrowOnError> =>
  (options.client ?? client).get<GetPresetResponses, GetPresetErrors, ThrowOnError>({
    url: '/v1/presets/{id}',
    ...options,
  });

/**
 * Upload presets to park for claiming
 *
 * Accepts preset YAML files, or a .zip laid out like the presets repository, validates each against the pinned schemas, and parks the result as a short-lived draft. Returns the draft id and the admin URL to claim it.
 */
export const uploadPresets = <ThrowOnError extends boolean = false>(
  options?: Options<UploadPresetsData, ThrowOnError>,
): RequestResult<UploadPresetsResponses, UploadPresetsErrors, ThrowOnError> =>
  (options?.client ?? client).post<UploadPresetsResponses, UploadPresetsErrors, ThrowOnError>({
    ...formDataBodySerializer,
    url: '/v1/uploads',
    ...options,
    headers: {
      'Content-Type': null,
      ...options?.headers,
    },
  });

/**
 * Read a parked upload
 *
 * Returns a parked upload draft so the admin app can render it for review. The id is the only capability required; ownership is checked at claim time.
 */
export const getUpload = <ThrowOnError extends boolean = false>(
  options: Options<GetUploadData, ThrowOnError>,
): RequestResult<GetUploadResponses, GetUploadErrors, ThrowOnError> =>
  (options.client ?? client).get<GetUploadResponses, GetUploadErrors, ThrowOnError>({
    url: '/v1/uploads/{id}',
    ...options,
  });

/**
 * Claim an upload and open a pull request
 *
 * Authorizes the caller's organization against the vendor manifest at the current head, re-validates the draft, and opens a pull request through the bot. Requires a Stytch session JWT.
 */
export const claimUpload = <ThrowOnError extends boolean = false>(
  options: Options<ClaimUploadData, ThrowOnError>,
): RequestResult<ClaimUploadResponses, ClaimUploadErrors, ThrowOnError> =>
  (options.client ?? client).post<ClaimUploadResponses, ClaimUploadErrors, ThrowOnError>({
    url: '/v1/uploads/{id}/claim',
    ...options,
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
  });

/**
 * List the vendor directory
 *
 * Returns a page of the vendor directory derived from the vendor.yaml manifests in the served catalog. The directory is cursor-paginated so it is never dumped in one response; the stytch_organization_id is not part of the public representation.
 */
export const listVendors = <ThrowOnError extends boolean = false>(
  options?: Options<ListVendorsData, ThrowOnError>,
): RequestResult<ListVendorsResponses, ListVendorsErrors, ThrowOnError> =>
  (options?.client ?? client).get<ListVendorsResponses, ListVendorsErrors, ThrowOnError>({
    url: '/v1/vendors',
    ...options,
  });
